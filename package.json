{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspaces foreach -A -p run lint",
    "test": "yarn workspaces foreach -A -p run test",
    "create-package": "yarn workspace create-tmeta-package run ts-node src/create.ts"
  },
  "packageManager": "yarn@4.12.0"
}
